import { redirect } from "next/navigation";

/** Retired — public sponsorship intake is closed pending written federal ethics approval. */
export default function SponsorshipRequestPage() {
  redirect("/sponsors");
}
