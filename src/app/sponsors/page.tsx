import { redirect } from "next/navigation";

/** Retired — public sponsorship recruitment is closed pending written federal ethics approval. */
export default function SponsorsPage() {
  redirect("/beneficiaries");
}
