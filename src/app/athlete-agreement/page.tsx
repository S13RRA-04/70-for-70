import { redirect } from "next/navigation";

/**
 * Retired — no longer publicly accessible or linked, pending written
 * federal ethics approval of any athlete-affiliation program. Content
 * preserved in git history and in src/lib/content/athlete-agreement.ts.
 */
export default function AthleteAgreementPage() {
  redirect("/mission");
}
