import { redirect } from "next/navigation";

/** Retired — public athlete-affiliation intake is closed pending written federal ethics approval. */
export default function JoinPage() {
  redirect("/mission");
}
